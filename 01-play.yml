- name: adding user
  hosts: web
  become: yes
  tasks:
   - name: adding user
     ansible.builtin.command: id expense
     register: user


   - name: printing
     ansible.buitlin.debug:
       msg:"{{user}}"   