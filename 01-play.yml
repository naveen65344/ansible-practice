- name: installing mysql if it is redhat
  hosts: web
  tasks:
   - name: installing mysql if it is redhat
     ansible.builtin.package:
       name: mysql
       state: present
     when: ansible_os_family=="RedHat"   