- name: installing nginx
  hosts: web
  become: yes
  tasks:
   - name: installinng ngix
     ansible.builtin.package:
       name: nginx
       state: present
   - name: starting server
     ansible.builtin.service:
       name: nginx
       state: started    
          